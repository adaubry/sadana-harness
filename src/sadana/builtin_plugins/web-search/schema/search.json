{
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "What to search for. Backend-supported operators such as site:example.com or \"exact phrase\" may work."
    },
    "count": {
      "type": "integer",
      "description": "How many results to return. Defaults to 5.",
      "minimum": 1,
      "maximum": 20,
      "default": 5
    }
  },
  "required": ["query"],
  "additionalProperties": false
}
