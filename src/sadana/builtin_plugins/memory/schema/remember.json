{
  "type": "object",
  "properties": {
    "entry_key": {
      "type": "string",
      "description": "A short, stable slug for this fact, e.g. \"dog_name\"."
    },
    "content": {
      "type": "string",
      "description": "The fact or decision to remember, in plain prose."
    },
    "kind": {
      "type": "string",
      "enum": ["decision", "preference", "fact"],
      "description": "What kind of thing this is, if you know: a decision, a preference, or a plain fact. Defaults to fact."
    }
  },
  "required": ["entry_key", "content"],
  "additionalProperties": false
}
